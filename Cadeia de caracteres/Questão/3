def programa():
          frase = str(input("Digite alguma frase: "))
          l= " "
          for c in frase:
                     if ord(c) == 32:
                             l = l
                     else:
                             l = l + c
          print(l)

programa()
